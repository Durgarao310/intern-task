>>> First we need to activate the virtual environment
>>> To activate the virtual environment with the help of myvenv folder and activate it 
>>> pip install -r requirements
>>> open the folder mysite 
>>> $ python manage.py migrate 
>>> $ python manage.py runserver 
>>> The application will start automatically in your browser on http://localhost:8000
